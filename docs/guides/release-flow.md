---
title: Release flow
description: Release flow for @haskou/flow.
---

# Release flow

1. Run `yarn test`.
2. Run `yarn build`.
3. Run `yarn docs:build`.
4. Update the version.
5. Publish the package.

Commits should follow conventional commits with gitmoji.
